# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
    title_en:
        label: Title (EN)
    title_fr:
        label: Title (FR)
    institution_en:
        label: Institution (EN)
    institution_fr:
        label: Institution (FR)
    level_en:
        label: Level (EN)
    level_fr:
        label: Level (FR)
    closing:
        label: Closing Date
        type: datetime
        format: Y-m-d
    order:
        label: Listing Order